import styled from "styled-components";
import "../../../styles/global"

export const StyledButtonCancel = styled.button`

    width: 7.080625em;
    height: 3em;

    background: var(--grey6);

    border: 0.09375em solid var(--grey6);
    border-radius: 0.25em;

    font-family: 'Inter';
    font-style: normal;
    font-weight: 600;
    font-size: 1em;

    color: var(--grey2);
`
export const StyledDeletdAd = styled.button`
    width: 11.8575em;
    height: 3em;

    background: var(--alert2);

    border: 0.09375em solid var(--grey6);
    border-radius: 0.25em;

    font-family: 'Inter';
    font-style: normal;
    font-weight: 600;
    font-size: 1em;

    color: var(--alert1);

    
`